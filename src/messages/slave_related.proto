// the master sends RegisterMasterMessage to a slave to require it to reregister
message ReregisterMasterMessage
{
  required string master_ip=1;
  required string port = 2;
  required string slave_ip =3;

}
